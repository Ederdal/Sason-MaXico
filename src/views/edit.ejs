<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.css" />
    <script src="https://code.jquery.com/jquery-1.11.1.min.js"></script>
    <script src="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.js"></script>
    <title>Tienda de abarrotes</title>
</head>

<body>
    <div data-role="footer">
        <h1>Abarrotes doña Pelos</h1>
    </div>
    <div data-role="navbar" data-iconpos="bottom">
        <ul>
            <li><a href="#" data-icon="shop" class="ui-btn-active">Products</a></li>
        </ul>
    </div>

    <div data-role="content">
        <form id="editForm" action="/<%= product.barcode %>" method="PUT">
            <input type="text" name="barcode" id="txt_codigo_barras" placeholder="Código" value="<%= product.barcode %>" readonly>
            <input type="text" name="name" id="txt_name" placeholder="Nombre" value="<%= product.name %>">
            <input type="text" name="description" id="txt_description" placeholder="Descripción" value="<%= product.description %>">
            <input type="number" name="price" id="txt_price" placeholder="Precio" value="<%= product.price %>">
            <label for="flip-2">Status:</label>
            <select name="status" id="flip-2" data-role="slider">
                <option value="0" <%= product.status == 0 ? "selected" : "" %>>Inactivo</option>
                <option value="1" <%= product.status == 1 ? "selected" : "" %>>Activo</option>
            </select>
            <button type="button" id="btnEdit" style="background-color: #0e50ea;"> Editar</button> 
            <a href="/" class="ui-btn" style="background-color: #f44336;">Cancelar</a> 
        </form>
    </div>

    <div data-role="footer">
        <h5>Department by Edgar Cruz Salas</h5>
    </div>

    <script>
       $(document).ready(function () {
    $('#btnEdit').click(function () {
        var formData = $('#editForm').serialize();

        $.ajax({
            url: $('#editForm').attr('action'),
            type: 'PUT',
            data: formData,
            success: function (data) {
                alert("Producto actualizado correctamente.");
                window.location.href = '/'; 
            },
        });
    });
});

    </script>

</body>

</html>
